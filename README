Install instructions

1. Add the path to this repo to your environment as EMACS_DIR

     export EMACS_DIR=~/git/dot-emacs

2. Install pyflakes - http://pypi.python.org/pypi/pyflakes

     easy_install pyflakes

3. Byte compile js2 mode

     M-x byte-compile-file RE path/to/js2.el RET

4. Check out git submodules

     g submodule update --init
